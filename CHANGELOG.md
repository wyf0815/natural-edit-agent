# Changelog

## 0.9.8

- 恢复 v9.5 中可用的确定性指令覆盖，并统一快路径、需求账本、协议参数默认值和权限审计语义。
- 修复全图 HEX 换色、全选、显示全部、文档旋转、适配选区、文字、USM、杂色和常用滤镜的规划退化。
- 按实际操作依赖读取 Photoshop 状态证据，复杂图层读取失败不再使整个文档状态失效。
- 执行时冻结用户已确认计划；选区恢复、修改、结果验收和提交在同一事务中完成，并增强失败回滚证明。
- 局部调整层复用获准选区并校验蒙版；原生滤镜改在副本执行，局部滤镜把选区固化为蒙版，避免修改源图层。
- 高置信度候选可自动锁定；低置信度同时支持点选、面板套索和 Photoshop 原生选区工具纠正。
- 质量警告可强制覆盖高置信度自动确认；点选纠错会写入权威选区会话，多目标任务可删除单个失败目标并保留成功候选。
- 红框、箭头等批注坐标增加本地画面指纹门禁，错误海报会在模型请求与 Photoshop 修改前被拒绝。
- 模型请求统一经过本地桥接，增加服务与厂商适配器构建哈希、图片解码尺寸上限和旧版本桥接令牌迁移。
- v9.8 安装继续使用稳定插件 ID，保留同一 UXP 本地存储中的模型设置和 API Key；发布审计改为递归精确清单。

## 0.9.7

- 将矩形、椭圆、多边形、主体、区域主体、颜色范围、语义对象和载入图层统一纳入候选确认门。
- 新增权威选区会话：人工修正和确认后锁定 Photoshop 当前选区，执行时恢复会话而不是重新计算。
- 新增面板自由套索替换、加选、减选和交集，并支持采用 Photoshop 原生工具修正后的当前选区。
- 新增高/中/低/无候选置信度策略；低置信度必须修正或显式接受。
- 坐标单位改为显式 `pixels`、`percent`、`normalized`，取消根据小数范围猜单位。
- 分割改用持久工作进程、串行限流、共享请求、客户端取消和裁剪 RLE 传输。
- 本地桥接新增随机令牌认证、受限 CORS、安全上游地址、配置 fail-closed、请求/响应边界和最小匿名健康信息。
- 每个计划操作强制关联用户需求，并检查遗漏要求、未授权操作和依赖关系。
- 增加 v9.7 发布脚本、严格模型哈希校验、桥接安全/并发/取消/裁剪 RLE/选区会话回归，以及独立 Photoshop 实机清单。
- 明确记录 API Key 和桥接令牌仍为本机明文的已知风险。

## 0.9.5

- 将自然语言规划与 Photoshop 执行彻底分离，计划只能调用 90 个注册能力。
- 新增完整对象和局部部件语义范围，避免把矩形定位框直接作为修改选区。
- 新增 MobileSAM 多候选合成、几何完整度、目标覆盖率、保护点和遮挡排除。
- 同一视觉目标最多执行两次有原因记录的视觉复核；取消静默模型重试。
- 新增目标候选人工替换、补选、排除和确认流程。
- 新增分割缓存，相同画面与参数不重复运行主体识别。
- 新增运行环境自检、请求预算、执行后逐项回读验收和受控回滚。
- 模型请求统一为 300 秒上限，并按厂商处理 temperature 与 JSON 模式差异。
- 支持 12 家模型服务和可配置的 Tokensea 本地兼容接口。
- 加入不含私有素材的完整奖杯分割回归样例与公开发布安全扫描。
