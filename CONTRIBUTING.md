# 贡献指南

感谢你改进 Natural Edit Agent。当前公开发布主线是 `uxp-v9.8`；历史测试只用于兼容性回归，不代表旧版插件源码随公开仓库发布。不要引入未注册的任意脚本执行。

## 开发流程

1. 从公开仓库创建分支。
2. 修改协议时，同步更新 `protocol.js`、`capabilities.js`、预检和验收逻辑。
3. 新增视觉行为时，至少提供一个公开、可重复的图像夹具和像素级断言。
4. 新增 v9.8 离线回归时使用 `tests/v98-*.test.js` 命名；`tests/run-v98-public.js` 会自动发现并运行全部这类测试。
5. 运行 `npm run check` 和 `npm test`；有 MobileSAM 模型时再运行相应的严格桥接/分割检查。
6. 运行 `npm run release:audit`，确认公开清单中没有额外文件、密钥、私有地址、PSD、日志或模型权重。

## 新增 Photoshop 能力

每项能力必须同时具备：

- 稳定的动作 ID 和参数契约；
- 目标类型与前置条件检查；
- 受控的 Photoshop 原生执行；
- 执行后从 Photoshop 回读的验收；
- 对失败和回滚的测试。

只增加提示词而没有执行器与验收，不视为新增能力。

## 报告问题

请提供 Photoshop 版本、插件版本、文档结构摘要、原始自然语言指令、执行计划、错误信息和脱敏截图。不要提交 API Key、私有 URL 或客户素材。
